meta {
  name: createSection
  type: http
  seq: 2
}

post {
  url: {{gatewayURL}}/projects/{{idProject}}/sections
  body: json
  auth: none
}

headers {
  Authorization: {{token}}
}

body:json {
  {
    "name": "Section de test"
  }
}

script:post-response {
  bru.setVar('idSection', res.body.token);
}
