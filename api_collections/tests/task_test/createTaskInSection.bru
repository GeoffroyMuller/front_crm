meta {
  name: createTaskInSection
  type: http
  seq: 3
}

post {
  url: {{gatewayURL}}/tasks
  body: json
  auth: none
}

headers {
  Authorization: {{token}}
}

body:json {
  {
    "name": "Tasks TEST",
    "idSection": 1
  }
}

script:pre-request {
  const idSection = bru.getVar('idSection');
  
  req.setBody({
    idSection,
    
  })
}
